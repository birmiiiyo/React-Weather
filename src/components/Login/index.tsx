import React from 'react'
import {Background} from './styles'

export const Login = () => {
    return (<Background>
        <button>login</button>
        <button>login</button>
        <button>login</button>
    </Background>)
}